import { OtpValidationResponse } from './otp-validation-response';

describe('OtpValidationResponse', () => {
  it('should create an instance', () => {
    expect(new OtpValidationResponse()).toBeTruthy();
  });
});
